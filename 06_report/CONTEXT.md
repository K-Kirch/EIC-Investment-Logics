# Stage 06 — Report

**Purpose:** LaTeX write-up of the thesis. Compiles to the submission PDF.

**Build:** `latexmk -pdf main.tex` from this directory. See the report's own `CLAUDE.md` (Layer 2, LaTeX-specific) for conventions and build instructions.

**Status:** placeholder — to be drafted with user approval. Contents are expected to arrive from `coding/my_report/` in Batch 05.
