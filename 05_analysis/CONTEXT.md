# Stage 05 — Analysis

**Purpose:** Applying the codebook (stage 04) to the corpus (stage 03). Produces the coded readthrough, the MAXQDA project, and the pattern-level observations that feed the Findings and Discussion chapters (Ch 4 + Ch 5).

**Workflow (per `CLAUDE.md`):**

1. Pass 1 — Deductive readthrough (main working document: `output/Readthrough_Notes_v1.md`)
2. Pass 2 — Inductive update (resolve `[?]` flags; develop pattern-level observations)
3. Pass 3 — Writing (feeds stage 06)

**Status:** placeholder — to be drafted with user approval.

## What lives where

- `output/` — canonical analytical artefacts (the readthrough notes, the MAXQDA project).
- `_scratch/` — working analytical thoughts not yet promoted to canonical observations.
